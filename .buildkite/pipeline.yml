agents:
  queue: mac
env:
  IMAGE_ID: $IMAGE_ID

steps:
  - label: Dummy
    command: echo "Hello, {{matrix}}!"
    matrix:
      - matrix value 1
      - matrix value 2
    notify:
      - github_commit_status:
          context: Dummy step with {{matrix}}
