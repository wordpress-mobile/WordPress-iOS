/// Abstraction representing and enforcing better support for accessibility
protocol Accessible {
    /// Prepares and Accessible element to provide rich support for voice over
    func prepareForVoiceOver()
}
