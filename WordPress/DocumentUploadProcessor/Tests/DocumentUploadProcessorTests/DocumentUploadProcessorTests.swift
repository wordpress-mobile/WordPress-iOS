import XCTest
@testable import DocumentUploadProcessor

final class DocumentUploadProcessorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DocumentUploadProcessor().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
