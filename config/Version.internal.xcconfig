// Include the default (public) version config to inherit all the default
// values, such as the version short...
#include "./Version.public.xcconfig"

// ...then override only those that should change for internal builds
VERSION_LONG=18.9.0.20211221
